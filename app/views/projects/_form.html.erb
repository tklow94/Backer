<%= form_with(model: project, local: true, multipart: true) do |form| %>
  <% if project.errors.any? class='p-6 mb-5 text-red-500 bg-white rounded'%>
    <div id="error_explanation">
      <h2 calss='font-bold text-red-500'><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% project.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-6">
    <%= form.label :title class: "label"%>
    <%= form.text_field :title class: "input" %>
  </div>

  <div class="mb-6">
    <%= form.label :donation_goal class: "label"%>
    <%= form.text_field :donation_goal class: "input" %>
  </div>

  <div class="mb-6">
    <%= form.submit calss: "btn btn-default" %>
  </div>
<% end %>
